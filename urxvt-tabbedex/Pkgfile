# Description: Tabbed plugin for rxvt-unicode with many enhancements
# URL:         https://github.com/mina86/urxvt-tabbedex
# Depends on:  rxvt-unicode

name=urxvt-tabbedex
version=20.09
release=1
source=(https://github.com/mina86/$name/releases/download/v$version/$name-$version.tar.bz2)

build() {
	cd $name-$version

	make DESTDIR=$PKG install
	rm -rf $PKG/usr/share/doc
}
